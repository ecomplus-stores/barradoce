<%
// store logo object
const logo = _.settings.logo
  ? { url: _.settings.logo, alt: _.settings.name }
  : _.store.logo
  
if (logo && logo.url) {
  const { width, height } = _.tryImageSize(logo.url)
  console.log(width, height)
  %>
  <img
    id="logo"
    class="header__logo lozad fade img-fluid"
    src="<%= logo.url %>"
    alt="<%= logo.alt %>"
    <% if (width) { console.log('estou dentro') %>
      width="<%= width %>"
      height="<%= height %>"
    <% } %>
  >
<% } else { %>
  <%= _.settings.name || _.store.name %>
<% } %>
